import 'package:flutter/material.dart';

class ChatMediaScreen extends StatelessWidget {
  const ChatMediaScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ChatMediaScreen'),
      ),
      body: const Center(
        child: Text('TODO'),
      ),
    );
  }

}