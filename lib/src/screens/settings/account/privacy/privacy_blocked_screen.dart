import 'package:flutter/material.dart';

class PrivacyBlockedScreen extends StatelessWidget {
  const PrivacyBlockedScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('TITLE'),
      ),
      body: const Center(
        child: Text('TODO'),
      ),
    );
  }

}